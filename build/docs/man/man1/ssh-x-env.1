.\" Automatically generated by Pandoc 2.17.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "ssh-x-env" "1" "" "Version Latest" "SSH-X Env"
.hy
.SH NAME
.PP
\f[V]ssh-x-env\f[R] - Print the env in an export format
.SS SYNOPSIS
.PP
Print the env for \f[V]SSH\f[R] and \f[V]SSH-X\f[R].
It\[cq]s used in the \f[V]ssh-x\f[R] scripts to set the environment.
.IP
.nf
\f[C]
eval $(ssh-x-env)
\f[R]
.fi
.SS ENV
.SS SSH-X
.IP \[bu] 2
\f[V]SSH_X_KEY_STORE\f[R]: the name of the key store that contains the
private key.
Possible values:
.RS 2
.IP \[bu] 2
\f[V]file\f[R]: (default) for the file system
.IP \[bu] 2
\f[V]pass\f[R]: for the pass password
manager (https://www.passwordstore.org/)
.IP \[bu] 2
\f[V]SSH_X_KEY_HOME\f[R]: a path to the directory containing your
private keys.
Default to:
.RS 2
.IP \[bu] 2
the absolute path \f[V]/home/admin/.ssh\f[R]: for the file key store:
.PD 0
.P
.PD
.IP \[bu] 2
the relative path \f[V]ssh-x\f[R] for the pass key store.
This path:
.RS 2
.IP \[bu] 2
relative to
PASSWORD_STORE_DIR (https://man.archlinux.org/man/extra/pass/pass.1.en#PASSWORD_STORE_DIR)
.IP \[bu] 2
should not be start with a point.
Otherwise, pass will not see it
.RE
.RE
.RE
.IP \[bu] 2
\f[V]SSH_X_LIFE\f[R]: the default lifetime of the loaded keys (Default
to \f[V]15m\f[R]) Used if a time interval is not found in the
\f[V]AddKeysToAgent\f[R] conf.
.IP \[bu] 2
\f[V]SSH_X_AGENT_ENV\f[R]: The location of the agent env file.
Default to \f[V]/home/admin/.ssh/ssh-x-agent.env\f[R]
.IP \[bu] 2
\f[V]SSH_X_KEY_PASSPHRASE_xxx\f[R]: The passphrase for a protected key
to be loaded when the agent starts.
See ssh-x-agent-init
.SS SSH
.PP
This utility prints also ssh
env (https://man.openbsd.org/ssh.1#ENVIRONMENT)
.IP \[bu] 2
SSH_AUTH_SOCK (https://man.openbsd.org/ssh.1#SSH_AUTH_SOCK): The
location of the agent socket Default to
\f[V]/home/admin/.ssh/agent.sock\f[R]
.IP \[bu] 2
SSH_ASKPASS (https://man.openbsd.org/ssh.1#SSH_ASKPASS) to set how to
retrieve the password without a terminal.
\f[V]ssh-x\f[R] has 2:
.RS 2
.IP \[bu] 2
ssh-x-askpass-env used to pass the password from a secret store
.IP \[bu] 2
ssh-x-askpass-prompt used to prompt the user for the secret
.RE
.IP \[bu] 2
SSH_ASKPASS_REQUIRE (https://man.openbsd.org/ssh.1#SSH_ASKPASS_REQUIRE)
used to require \f[V]SSH_ASKPASS\f[R] behavior
.SS Key Store
.PP
We support 2 keys store: * \f[V]file\f[R]: where the key is stored in a
file system directory (default) * \f[V]pass\f[R]: where the key is
stored in the pass password manager (https://www.passwordstore.org/)
.PP
You choose your key store with the variable SSHX_KEY_STORE
.PP
For the 2 stores, the key is located at \f[V]/KeyName\f[R] where : *
SSHX_KEY_HOME defines the directory location.
* \f[V]KeyName\f[R] is the name of the private key (ie the public key
without the \f[V].pub\f[R] extension)
